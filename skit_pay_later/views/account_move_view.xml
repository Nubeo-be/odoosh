<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- VIEWS -->

        <!-- account.move.line (Journal items) -->
       <record id="view_move_line_paylater" model="ir.ui.view">
            <field name="name">account.move.line.paylater.tree</field>
            <field name="model">account.move.line</field>
            <field name="mode">primary</field>
            <field name="inherit_id" ref="account.view_move_line_tree"/>
            <field name="arch" type="xml">
	           <field name="date" position="after">
	                   <field name="is_pay_later" string="Pay Later" optional="show" />
	                   <field name="paylater_amt" sum="PayLater Amt"  optional="show" />	                  
	           </field>
            </field>
         </record>
         
         
          <record model="ir.ui.view" id="view_move_line_form_paylater">
            <field name="name">account.move.line.paylater</field>
            <field name="model">account.move.line</field>
            <field name="inherit_id" ref="account.view_move_line_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='blocked']" position="before">
                    <field name="is_pay_later"/>
                    <field name="paylater_amt"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>